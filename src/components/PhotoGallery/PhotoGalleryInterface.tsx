import CRTDisplay from '@/components/CTRDisplay/CTRDisplay';
import PhotoGalleryContent from './PhotoGalleryContent';

const PhotoGalleryInterface = () => {
  return (
    <CRTDisplay>
      <PhotoGalleryContent />
    </CRTDisplay>
  );
};

export default PhotoGalleryInterface;
